module.exports = {
  options: {
    name: 'name',
    aliases: [ 'alias', 'alias2' ],
    category: 'Public',
    requiredPerm: false,
    disable: true
  },
  run: async (MessageEmbed, client, message, args, env, prefix, servers) => {
    
  }
}